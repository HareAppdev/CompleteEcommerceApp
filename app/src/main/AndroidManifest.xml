<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myecommerceapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyEcommerceApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".myEcommerce.activities.CartActivity"
            android:exported="false"
            android:theme="@style/Theme.MyEcommerceApp.ActionBar"/>
        <activity
            android:name=".myEcommerce.activities.ProductDetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.MyEcommerceApp.ActionBar"
            android:label="Shoping Cart"/>
        <activity
            android:name=".myEcommerce.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>